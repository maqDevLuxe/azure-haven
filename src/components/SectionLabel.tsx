const SectionLabel = ({ children }: { children: string }) => (
  <span className="text-xs tracking-[0.35em] uppercase text-muted-foreground font-light block mb-4">
    {children}
  </span>
);

export default SectionLabel;
